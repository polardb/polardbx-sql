emp:
  CREATE TABLE emp(
  EMPNO INT NOT NULL PRIMARY KEY,
  ENAME VARCHAR(20) NOT NULL,
  JOB VARCHAR(10) NOT NULL,
  MGR INT NULL,
  HIREDATE TIMESTAMP NOT NULL,
  SAL INT NOT NULL,
  COMM INT NOT NULL,
  DEPTNO INT NOT NULL,
  SLACKER TINYINT(1) NOT NULL
  ) DBPARTITION BY HASH(EMPNO) TBPARTITION BY HASH(EMPNO) TBPARTITIONS 7

dept:
  CREATE TABLE dept(
  DEPTNO INT NOT NULL PRIMARY KEY,
  NAME VARCHAR(10) NOT NULL
  ) DBPARTITION BY HASH(DEPTNO) TBPARTITION BY HASH(DEPTNO) TBPARTITIONS 7

bonus:
  CREATE TABLE bonus(
  ENAME VARCHAR(20),JOB VARCHAR(10),SAL INT,COMM INT)

dept_broadcast:
  CREATE TABLE dept_broadcast(
  DEPTNO INT NOT NULL PRIMARY KEY,
  NAME VARCHAR(10) NOT NULL
  ) BROADCAST

emp_single:
  CREATE TABLE emp_single(
  EMPNO INT NOT NULL PRIMARY KEY,
  ENAME VARCHAR(20) NOT NULL,
  JOB VARCHAR(10) NOT NULL,
  MGR INT NULL,
  HIREDATE TIMESTAMP NOT NULL,
  SAL INT NOT NULL,
  COMM INT NOT NULL,
  DEPTNO INT NOT NULL,
  SLACKER TINYINT(1) NOT NULL
  )

dept_single:
  CREATE TABLE dept_single(
  DEPTNO INT NOT NULL PRIMARY KEY,
  NAME VARCHAR(10) NOT NULL
  )

emp0:
  CREATE TABLE emp0(
  EMPNO INT NOT NULL PRIMARY KEY,
  ENAME VARCHAR(20) NOT NULL,
  JOB VARCHAR(10) NOT NULL,
  MGR INT NULL,
  HIREDATE TIMESTAMP NOT NULL,
  SAL INT NOT NULL,
  COMM INT NOT NULL,
  DEPTNO INT NOT NULL,
  SLACKER TINYINT(1) NOT NULL
  ) DBPARTITION BY HASH(EMPNO) TBPARTITION BY HASH(EMPNO) TBPARTITIONS 6

dept0:
  CREATE TABLE dept0(
  DEPTNO INT NOT NULL PRIMARY KEY,
  NAME VARCHAR(10) NOT NULL
  ) DBPARTITION BY HASH(DEPTNO) TBPARTITION BY HASH(DEPTNO) TBPARTITIONS 7

t1:
  create table t1(
  a varchar(30), 
  b varchar(30)
  ) dbpartition by hash(`a`) tbpartition by hash(`b`) tbpartitions 8;

t2:
  create table t2(
  a varchar(30), 
  b varchar(30)
  ) dbpartition by hash(`b`) tbpartition by hash(`a`) tbpartitions 8;
