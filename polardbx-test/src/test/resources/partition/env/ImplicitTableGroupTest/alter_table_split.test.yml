## DISABLE_FAST_SQL_PARSER
CREATE TABLE `rng_t1` (
`a` varchar(32) DEFAULT NULL,
`b` int DEFAULT NULL,
`_drds_implicit_id_` bigint NOT NULL AUTO_INCREMENT,
PRIMARY KEY (`_drds_implicit_id_`),
LOCAL KEY `auto_shard_key_a_b` USING BTREE (`a`, `b`)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4
PARTITION BY RANGE COLUMNS(`a`,`b`)
(PARTITION `p1` VALUES LESS THAN ('abc',MAXVALUE) ENGINE = InnoDB,
PARTITION `p2` VALUES LESS THAN ('efg',MAXVALUE) ENGINE = InnoDB,
PARTITION `p3` VALUES LESS THAN ('hij',MAXVALUE) ENGINE = InnoDB);

CREATE TABLE `rng_t2` (
`a` varchar(32) DEFAULT NULL,
`b` int DEFAULT NULL,
`_drds_implicit_id_` bigint NOT NULL AUTO_INCREMENT,
PRIMARY KEY (`_drds_implicit_id_`),
LOCAL KEY `auto_shard_key_a_b` USING BTREE (`a`, `b`)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4
PARTITION BY RANGE COLUMNS(`a`,`b`)
(PARTITION `p1` VALUES LESS THAN ('abc',MAXVALUE) ENGINE = InnoDB,
PARTITION `p2` VALUES LESS THAN ('efg',MAXVALUE) ENGINE = InnoDB,
PARTITION `p3` VALUES LESS THAN ('hij',MAXVALUE) ENGINE = InnoDB);

create table `rng_t3` like rng_t1;
create tablegroup my_rng_tg1;
/*+TDDL:cmd_extra(ENABLE_PHY_RECYCLEBIN=TRUE)*/ALTER TABLE rng_t1 SPLIT PARTITION p3 INTO (PARTITION p3_1 VALUES LESS THAN ('hhh'), PARTITION p3_2 VALUES LESS THAN ('hij')) WITH TABLEGROUP=tg12345 IMPLICIT;
ALTER TABLE rng_t2 SPLIT PARTITION p3 INTO (PARTITION p3_1 VALUES LESS THAN ('hhh'), PARTITION p3_2 VALUES LESS THAN ('hij')) WITH TABLEGROUP=tg12345 IMPLICIT;
ALTER TABLE rng_t3 SPLIT PARTITION p3 INTO (PARTITION p3_1 VALUES LESS THAN ('hhh'), PARTITION p3_2 VALUES LESS THAN ('hij')) WITH TABLEGROUP=my_rng_tg1 IMPLICIT;
/*+TDDL:cmd_extra(SHOW_HASH_PARTITIONS_BY_RANGE=TRUE)*/ show create table rng_t1;
/*+TDDL:cmd_extra(SHOW_HASH_PARTITIONS_BY_RANGE=TRUE)*/ show create table rng_t2;
/*+TDDL:cmd_extra(SHOW_HASH_PARTITIONS_BY_RANGE=TRUE)*/ show create table rng_t3;