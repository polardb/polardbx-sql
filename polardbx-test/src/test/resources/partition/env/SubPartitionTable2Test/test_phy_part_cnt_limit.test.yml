## DISABLE_FAST_SQL_PARSER

  ## error_msg: $# Too many #$
CREATE TABLE `t3` ( `a` int(11) DEFAULT NULL, `b` int(11) DEFAULT NULL)
ENGINE = InnoDB DEFAULT CHARSET = utf8mb4
PARTITION BY KEY(`a`) PARTITIONS 512
SUBPARTITION BY KEY(`b`) SUBPARTITIONS 512;

  ## error_msg: $# Too many #$
CREATE TABLE `t3` ( `a` int(11) DEFAULT NULL, `b` int(11) DEFAULT NULL)
ENGINE = InnoDB DEFAULT CHARSET = utf8mb4
PARTITION BY KEY(`a`) PARTITIONS 2
SUBPARTITION BY KEY(`b`) SUBPARTITIONS 5120;


  ## error_msg: $# Too many #$
CREATE TABLE `t3` ( `a` int(11) DEFAULT NULL, `b` int(11) DEFAULT NULL)
ENGINE = InnoDB DEFAULT CHARSET = utf8mb4
PARTITION BY KEY(`a`) PARTITIONS 5120
SUBPARTITION BY KEY(`b`) SUBPARTITIONS 2;
